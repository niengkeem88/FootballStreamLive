<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="keemgames.footballcompanion.FootballCompanionApp"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.FootballPulseLive">

        <!-- AppLovin MAX SDK Key (Mandatory for initialization) -->
        <meta-data
            android:name="applovin.sdk.key"
            android:value="${applovinSdkKey}" />

        <activity
            android:name="keemgames.footballcompanion.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FootballPulseLive">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
